// Configuration file for API URLs
// When deployed, process.env.REACT_APP_API_URL should be set in the build environment.
// For development, it defaults to localhost.

export const API_BASE_URL = process.env.REACT_APP_API_URL || "http://localhost:5050";
